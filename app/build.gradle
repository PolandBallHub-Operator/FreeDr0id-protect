plugins { id 'com.android.application' }

android { namespace 'com.google.android.verifier'; compileSdk 35
    buildFeatures { buildConfig true }
    defaultConfig { applicationId 'com.google.android.verifier'; minSdk 28; targetSdk 35; versionCode 2; versionName 'September302026notcoming' }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.12.0'
    implementation project(':freeDroidWarn')
}
